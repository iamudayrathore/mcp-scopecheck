# MCP ScopeCheck v0.1.1

This patch updates the repository owner identity and adds the reviewed PyPI Trusted Publishing workflow.

- Repository metadata now points to `https://github.com/iamudayrathore/mcp-scopecheck`.
- PyPI publication is constrained to the `release.yml` workflow and `pypi` GitHub environment.
- Package name, CLI name, runtime dependencies, scanner behavior, rule output, fixtures, and security boundary are unchanged from v0.1.0.

The existing v0.1.0 tag, GitHub release, and artifacts remain unchanged.
